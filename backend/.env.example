# Server
PORT=5000
NODE_ENV=development

# JWT
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production
JWT_EXPIRES_IN=24h

# Database (PostgreSQL)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=scalable_rest_api
DB_USER=postgres
DB_PASSWORD=your_password
DB_DIALECT=postgres
